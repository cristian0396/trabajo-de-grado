<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Proyecto.Vistas.RecorridoItemPage">
    <!--Contenido principal del carrouselView (imagen, subtitulo, texto) de la introducción de cada modulo-->
  <ContentView.Content>
        <ScrollView>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Image Grid.Row="0" Source="{Binding DirImagen}" Margin="0,30,0,0"/>
                <Label Grid.Row="1" Text="{Binding Titulo}" FontSize="30" TextColor="white" Margin="20,30,0,20"/>            
                <Label Grid.Row="2" Text="{Binding Contenido}" FontSize="20" TextColor="white" Margin="20,0,0,20" />                
            </Grid>
        </ScrollView>
    </ContentView.Content>
</ContentView>