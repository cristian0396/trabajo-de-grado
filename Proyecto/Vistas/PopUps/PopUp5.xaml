<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
             mc:Ignorable="d"
             x:Class="Proyecto.Vistas.PopUps.PopUp5">
    <!--Ventana emergente de GASTOS FIJOS-->
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="Contenido" TargetType="ContentPage" ApplyToDerivedTypes="True">
                <Setter Property="BackgroundColor" Value="Transparent"></Setter>
            </Style>
            <Style x:Key="Message" TargetType="Label">
                <Setter Property="FontSize" Value="18"></Setter>
                <Setter Property="TextColor" Value="Black"></Setter>
                <Setter Property="HorizontalTextAlignment" Value="Center"></Setter>
                <Setter Property="Margin" Value="0,0,0,20"></Setter>
            </Style>
            <Style x:Key="Flecha" TargetType="Image" ApplyToDerivedTypes="True">
                <Setter Property="Rotation" Value="-90"></Setter>
                <Setter Property="Scale" Value="1"></Setter>
                <Setter Property="Source" Value="flecha.png"></Setter>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content Style="{StaticResource Contenido}">
        <Grid RowSpacing="0">
            <Grid.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding CloseCommand}" />
            </Grid.GestureRecognizers>
            <Grid.RowDefinitions>
                <RowDefinition Height="248"/>
                <RowDefinition Height="170"/>
                <RowDefinition Height="180"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Image Grid.Row="1" Grid.Column="0" x:Name="imgFlecha" Style="{StaticResource Flecha}"></Image>
            <Label Grid.Row="2" Grid.ColumnSpan="2" Text="{Binding Message}" Style="{StaticResource Message}"/>
        </Grid>
    </ContentPage.Content>
</pages:PopupPage>